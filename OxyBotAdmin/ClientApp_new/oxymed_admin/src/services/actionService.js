import Vue from "vue";

export default {

    getActions(beginPage, endPage, url=""){
        try {
            
        } catch (error) {
            
        }
    },

    saveAction(actionData, url=""){
        try {
            
        } catch (error) {
            
        }
    }
}